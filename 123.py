test = np.reshape([
test
np.mean(test,axis=0)
np.cov(test)
np.cov(test, T)
density =pdf.(cv_data,mu, sigma)
T= (min(destiny), max(desti))
density[0.1,0.2,0.05,0.01,....0.5]1X370(compare threshold)
t1=0.01
t2=0.02
.
.
.
.
.
binary[0,0,0,1,,10....]
t = best largest number will e our threshold number



